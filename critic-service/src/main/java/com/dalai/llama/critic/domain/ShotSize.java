package com.dalai.llama.critic.domain;

/** Wire-contract copy of video-generation-service's {@code ShotSize} enum names -- this build's
 * own convention: each service owns its copy of a contract it depends on. */
public enum ShotSize {
    EWS, VWS, WS, MWS, MS, MCU, CU, ECU, INSERT, OTS
}
