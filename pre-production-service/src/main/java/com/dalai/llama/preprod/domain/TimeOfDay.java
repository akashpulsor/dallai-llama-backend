package com.dalai.llama.preprod.domain;

/** Wire-contract copy of video-generation-service's {@code TimeOfDay} enum names. */
public enum TimeOfDay {
    DAWN,
    GOLDEN_HOUR,
    MIDDAY,
    BLUE_HOUR,
    NIGHT,
    MAGIC_HOUR
}
