package com.dalai.llama.preprod.domain;

/** Wire-contract copy of video-generation-service's {@code MoodProfile} enum names. */
public enum MoodProfile {
    HIGH_KEY,
    LOW_KEY,
    CHIAROSCURO,
    SOFT
}
