package com.dalai.llama.preprod.service.critic;

/** Wire-contract copy of critic-service's {@code CriticRole} enum names. */
public enum CriticRole {
    HARD_CONSTRAINTS,
    DIRECTOR,
    DP,
    PRODUCTION_DESIGN,
    GENERATION_FEASIBILITY
}
