package com.dalai.llama.preprod.domain;

/** Wire-contract copy of video-generation-service's {@code AnchorType} enum names. */
public enum AnchorType {
    GLOBAL_CAMPAIGN,
    LOCAL_SCENE,
    PROP,
    PRIOR_SHOT
}
